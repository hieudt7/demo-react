import {
} from './actions'

const initialState = {
  loading: false,
}

export default function orderReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
